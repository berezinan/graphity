# Правила графа для этого репозитория. Действуют и при сканировании из
# родительского каталога, где .gitignore этого репозитория не применяется
# (умолчание gitignore-off-by-default).

# Скачанный сервер ArcadeDB и синтетический граф спайка (synth_1m.json ~331 МБ)
spikes/arcade/.arcade/
spikes/arcade/*.json

# Сборочный вывод .NET в фикстурах тестов
tests/fixtures/obj/
